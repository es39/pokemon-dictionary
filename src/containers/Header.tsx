import styled from 'styled-components';

const Header = () => {
  return (
    <HeaderStyle>
      <div></div>
    </HeaderStyle>
  );
};

export default Header;

const HeaderStyle = styled.header`
  width: 100vw;
  height: 60px;
  background: red;
`;
